#include <iostream>

using namespace std;

int main()
{
	int a = 5;
	int b = 2;

	bool total1, total2;

	// Operator Assignment : =, +=, -=, *=, /=, %=, &=, |=, ^=, >>=, <<=
	// 1. (+=)
	a += 5;
	b += 2;
	cout << a;

	// 2. (-=)
	a -= 2;
	b -= 5;

	// 3. (*=)
	a *= 5;
	b *= 2;

	// 4. (/=)
	a /= 5;
	b /= 2;

	// 5. (%=)
	a /= 5;
	b /= 2;


	cin.get();
	return 0;
}